#!/usr/bin/env sh
# Pre-commit gate — runs ESLint + Prettier on staged files via lint-staged.
# Typecheck + tests are NOT run here (too slow for every commit) — CI catches
# those on push. To bypass in an emergency: `git commit --no-verify` (don't
# make a habit of it).

pnpm exec lint-staged
